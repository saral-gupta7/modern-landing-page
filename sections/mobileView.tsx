import React from "react";

const MobileView = () => {
  return (
    <section className="flex-center h-screen max-w-screen bg-black text-4xl text-wrap text-white">
      The mobile page in under construction! 👷‍♂️🚧
    </section>
  );
};

export default MobileView;
